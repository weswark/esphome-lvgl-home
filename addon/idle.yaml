# Starts screen saver
#lvgl:
#  on_idle: 
#    timeout: "10s"
#    then:
#      - logger.log: "Screen is idle"
#      - light.turn_off: display_backlight
#      - lvgl.pause:
#          show_snow: true

# Starts screen saver
switch:
  - platform: template
    name: "Screensaver"
    id: screensaver
    turn_on_action:
      - logger.log: "Screensaver is turned on"
      - light.turn_off: display_backlight
      - lvgl.pause:
          show_snow: true
    turn_off_action:
      - logger.log: "Screensaver is turned off"
      - light.turn_on: display_backlight
      - lvgl.resume:

# Stops screen saver
touchscreen:
  on_touch:
    then:
      - logger.log: "Screen is active"
      - lvgl.resume:
      - light.turn_on: display_backlight

