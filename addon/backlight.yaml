output:
  - platform: gpio
    id: backlight_output
    pin:
      ch422g: io_ex
      number: 2

light:
  - platform: monochromatic
    id: backlight_light
    name: Back Light
    entity_category: config
    output: backlight_output
    restore_mode: ALWAYS_ON
