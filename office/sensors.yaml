binary_sensor:
  - platform: homeassistant
    id: homeassistant_office_lamp_light
    entity_id: light.office_lamp_light
    publish_initial_state: true
    on_state:
      then:
        lvgl.widget.update:
          id: office_lamp_light
          state:
            checked: !lambda return x;
